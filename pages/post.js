import Layout from '../components/Layout'

const Post = ({ url }) => (
    <Layout title={url.query.title}>
        <p>
            asdasdad addadasdsad
        </p>
    </Layout>
)

export default Post